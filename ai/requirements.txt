# AI module (Python 3.11+)
# Install: pip install -r requirements.txt
# Run: see ai/README.md

# --- Serving runtime (uvicorn app.main:app) ---
# GovGuard v1.0.4 is a single file (scripts/gov_v1_0_4.py);
# scikit-learn and matplotlib are also required at serve time.
pandas>=2.0.0
numpy>=1.24.0
pyarrow>=14.0.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0
httpx>=0.27.0
python-dotenv>=1.0.0

# --- Batch jobs (e.g. scripts/gov_batch_forecast.py -> MySQL) ---
# Needs DATABASE_URL (mysql://...) from env or backend/.env
PyMySQL>=1.1.0

# --- Training / experiments / notebooks / benchmarks ---
seaborn>=0.13.0
psutil>=5.9.0

# Note: PDF generation moved to Backend (Express + Playwright + EJS).
# Do not install playwright/jinja2 for AI serve.
